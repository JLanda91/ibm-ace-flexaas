apiVersion: v1
kind: PersistentVolumeClaim
metadata:
 name: inputmsg-api-pvc
 labels:
   app: inputmsg-api
spec:
 accessModes:
   - ReadWriteMany
 resources:
   requests:
     storage: 10Gi
 storageClassName: ibmc-file-retain-gold